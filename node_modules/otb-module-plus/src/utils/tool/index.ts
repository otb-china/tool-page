import {
  day,
  monthNumber,
  yearNumber,
  howLongAgo,
  timestamp,
  timeDifference
} from "./date"
import { exclude, regular, validator } from "./check"
import { fileLinkFileType, fileSize } from "./file"
import { reject, resolve } from "./Promise"
import { add, div, mul, sub, toThousandsString } from "./math"
import { go } from "./route"
import { getObject, getSliceArray, sort, treeSort, type } from "./data"

export {
  day,
  monthNumber,
  yearNumber,
  howLongAgo,
  timestamp,
  timeDifference,
  regular,
  fileSize,
  fileLinkFileType,
  resolve,
  reject,
  add,
  sub,
  mul,
  div,
  toThousandsString,
  type,
  getObject,
  sort,
  treeSort,
  getSliceArray,
  exclude,
  validator,
  go
}
