// Promise resolve
export const resolve = (params: unknown) => {
  return new Promise((resolve) => {
    resolve(params)
  })
}

// Promise reject
export const reject = (params: any) => {
  // @ts-ignore
  return new Promise((resolve, reject) => {
    reject(params)
  })
}
