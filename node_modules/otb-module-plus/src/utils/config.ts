import { RSS } from "./type"
// 配置数据

// 默认的附件上传地址
export let upLoadLink: string = ""

// 配置默认的附件上传地址
export const setUpLoadLink = (link: string) => {
  upLoadLink = link
}

// 统一的路由跳转数据
export let linkList: RSS[] = []

// 配置统一的路由跳转数据
export const setLinkList = (list: RSS[]) => {
  linkList = list
}
