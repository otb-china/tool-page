import { RSA } from "./type";
declare class SStorageConstructor {
    key: string;
    constructor(key: any);
    setter(data: RSA | RSA[] | string | number): void;
    getter(): any;
    remove(): void;
}
export declare const SStorage: {
    loginBack: SStorageConstructor;
    headerTags: SStorageConstructor;
    new: (key: string) => SStorageConstructor;
};
export {};
